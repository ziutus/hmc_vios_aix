
HMC;          event time;ID;  PHM;sys name;text<% events.events.each do |entry| %>
<%= entry.hmc_name %>;<%= entry.last_time %>;<%=  entry.problem_num %>;<%= sprintf("%5s",entry.pmh_num) %>;<%= entry.sys_name %>;<%= entry.text %>;<% end %>

