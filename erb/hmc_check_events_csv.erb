HMC;          event time;ID;  PHM;sys name;text<% events2.each do |entry| %>
<%= entry.hmcs_name.join(',') %>;<%= entry.last_time %>;<%=  entry.problem_num %>;<%= sprintf("%5s",entry.pmh_num) %>;<%= entry.refcode %>;<%= entry.sys_name %>;<%= entry.failing_mtms %>;<%= entry.text %>;<% end %>

