HMC;          event time;ID;  PHM;refcode; system serial; sys name; failing mtms; machine type; model; priority; ticket number; event status; Resolution Status; text; action plan;
<% events2.each do |entry| %><%= entry.hmcs_name.join(',') %>;<%= entry.last_time %>;<%=  entry.problem_num %>;<%= entry.pmh_num_nice %>;<%= entry.refcode %>;<%= entry.serial %>;<%= entry.sys_name %>;<%= entry.failing_mtms %>;<%= entry.machine_type %>;<%= entry.machine_model %>;<%= entry.priority %>;<%= entry.ticket_number %>;<%= entry.status %>;<%= entry.resolution_status %>;<%= entry.text %>;<%= entry.action_plan %>;
<% end %>